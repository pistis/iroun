pkg banner banner banner output Dir output output Min meta options options options options options options options options files files files files files files dist dist rig report uglify jshintrc src src prebuild tests jshint frameworks browsers browsers background reporters unit single Run continuous karma Begin tasks watch exports Debouncer update request Tick handle callback request Animation callback ticking ticking ticking raf Callback DOM extend normalize Tolerance Headroom init destroy attach unpin pin bottom Bottom Viewport Height Physical Height Scroller Physical Height Height Height Scroller Height Bounds tolerance Exceeded Unpin Pin update freeze unfreeze result replacement classes classes classes classes classes classes classes List List List List List List body past past Bottom scrolling past Offset past Offset scrolling current Direction tolerance Exceeded obj elem options options current current current current Direction tolerance Exceeded tolerance Exceeded elem tolerance tolerance classes classes offset offset scroller scroller initialised initialised initialised Pin Unpin Bottom Bottom frozen frozen frozen frozen debouncer pinned unpinned bottom Bottom initial options cuts Mustard headroom Headroom Service options opts headroom Headroom Service headroom Headroom Service tolerance offset classes scroller scroller scope prop features bind List AF factory exports Headroom data options options data headroom